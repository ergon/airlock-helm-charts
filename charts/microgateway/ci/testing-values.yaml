image:
  repository: orltom/microgateway
config:
  generic:
    existingSecret: "microgatewaysecrets"
imagePullSecrets:
  - name: "dockersecret"
redis:
  enabled: true
echo-server:
  enabled: true
ingress:
  enabled: true
