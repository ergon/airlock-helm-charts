image:
  repository:
    runtime: docker.io/ergon/airlock-microgateway-nightly
    configbuilder: docker.io/ergon/airlock-microgateway-configbuilder-nightly
  tag: 2.1.0.sprint4_Build004
  pullPolicy: Always

config:
  dsl:
    apps:
      - virtual_host:
          hostname: echo
        mappings:
          - name: echo
            entry_path: /dsl/
            backend:
              hosts:
                - protocol: http
                  name: dsl-backend
                  port: 8080
